{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug FastAPI Project backend: Python Debugger",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "app.main:app",
        "--reload"
      ],
      "envFile": "${workspaceFolder}/debug.env",
      "preLaunchTask": "Generate env from 1Password"
    }
  ]
}
